name: Deploy Index File

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Code Download Karo
        uses: actions/checkout@v4

      - name: File Copy Karo
        run: cp index.html /var/www/html/
